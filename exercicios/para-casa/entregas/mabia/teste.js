

var A = prompt("Insira o primeiro número  ");
var B = prompt("Insira o segundo  ");

function soma() {
  var X = parseInt(A) + parseInt(B);
  console.log(`A soma entre ${A} e ${B} é ${X}`);
}

soma();
